import { END_MARKER } from "../constants.js";

/**
 * Strip the end of a string if it ends with {@linkcode END_MARKER}
 * @param {string} text
 * @param {string} endMarker
 * @returns {string}
 */
export const stripRemoveEnd = (text: string): string => {
  throw new Error("Function 'stripRemoveEnd' not implemented.");
};
